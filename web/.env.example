# API base for browser requests
# Production: usually /api (through nginx reverse proxy)
VITE_API_BASE=/api

# Used only by Vite dev server proxy
VITE_DEV_PROXY_TARGET=http://localhost:3000

# Frontend ports
VITE_DEV_PORT=5173
VITE_PREVIEW_PORT=4173
