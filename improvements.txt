2) Синхронизировать превью и серверный рендер (P1)
Сейчас превью во фронте и обработка на сервере реализованы разными формулами:

Live preview: App.vue:4188
Apply edits на сервере: media.controller.ts:1565
Риск: “в предпросмотре одно, после Save другое”.

Что сделать:

Вынести единую модель параметров (диапазоны, дефолты, кламп) в shared контракт.
Добавить endpoint preview-render (низкое качество, быстрый ответ) для сложных эффектов, где CSS неточно имитирует Sharp.
Для каждого инструмента определить режим: Preview via CSS или Preview via server.


[+] 4) Быстрые UX-улучшения редактора (P1/P2)
У вас уже хорошая база (много ползунков, crop/rotate/flip), поэтому следующий шаг:

Presets: Auto, Portrait, Landscape, Night, BW.
Copy/Paste edits между фото.
Before/After по удержанию клавиши (или long-press на мобильном).
Reset по группам (Tone, Detail, Color, Geometry), а не только общий reset.
Гистограмма и подсветка clipping (пересвет/провал в тени).




5) Производительность и стабильность на больших фото (P2)

Очередь задач редактирования на сервере (чтобы не забить CPU при параллельных save).
Ограничение max мегапикселей и безопасный downscale для очень больших исходников.
Кэш превью по ключу параметров: mediaId + hash(adjustments).
Фоновая генерация нескольких размеров превью после save.






[+] 6) Архитектура фронта (P2)
Редактор сейчас встроен в очень большой App.vue. Это тормозит развитие.

Вынести редактор в отдельный компонент и composable:
useEditorState
useEditorPreview
useEditorActions
Это упростит тесты, добавление инструментов и мобильную версию.




[+] Если хочешь, следующим шагом могу:

Добавить Web Worker для обработки пикселей (ещё меньше лагов на больших фото).
Перевести тяжёлые операции на WebGL (ещё быстрее и плавнее).



Если хочешь, следующим шагом добью до еще более “Pixlr-like” уровня:
Маски и локальные кисти (локальные правки по областям)
Быстрые one-tap эффекты пакетом (Cinematic, Vintage, Matte, Teal&Orange, Soft Skin) с отдельной силой эффекта


Если хочешь, следующим шагом добью до еще более “премиального” уровня:
Интенсивность quick recipes (ползунок силы эффекта 0-100 для каждого рецепта)
Локальные маски (кисть/ластик) для выборочной коррекции


Если хочешь, следующим шагом могу:
Сделать еще более радикальный UX-pass под mobile-first (крупные контролы, меньше табов, контекстные панели).
Добавить отдельный режим “Liquify workspace” (без лишних контролов, только кисть/интенсивность/undo/redo).



[+] На фронте отправлять батчами по суммарному размеру (например 20-40 MB на запрос), а не одной огромной пачкой.
[+] На сервере ограничить параллелизм (очередь/пул 2-4 файла), убрать глобальный Promise.all на всю пачку.
Поднять max_memory_restart (например 800M-1G) и при необходимости добавить NODE_OPTIONS с лимитом heap.
Для видео сделать отдельный путь загрузки (streaming/resumable), а не общий путь как для фото.
Проверить, нет ли внешнего прокси (например Cloudflare) с собственным лимитом тела запроса.
